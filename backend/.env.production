# ─────────────────────────────────────────────────────────
# .env.production — Template environment variables untuk Cloud Run
# JANGAN commit file ini ke git!
# Isi nilai ini saat deploy menggunakan --set-env-vars di gcloud CLI
# ─────────────────────────────────────────────────────────

# Database Configuration (MySQL yang disediakan)
DB_HOST=34.172.113.167
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=mypassword
DB_NAME=notes_XXXXXXXXX

# Server Configuration
# Cloud Run otomatis set PORT=8080, tapi bisa override di sini
PORT=8080
